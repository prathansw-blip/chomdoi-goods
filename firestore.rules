rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /stores/{storeId} {
      // อ่านได้ทุกคน (login page ต้องแสดง logo/ชื่อ), เขียนต้อง login
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
