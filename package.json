{
  "name": "chomdoi-goods",
  "private": true,
  "version": "2.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "dev:emulator": "firebase emulators:exec --config firebase.secure.emulator.json --project demo-chomdoi-tests --only auth,firestore \"node scripts/seed-emulator.mjs && vite --mode emulator\"",
    "test:emulator": "vitest run tests",
    "test:emulator:run": "firebase emulators:exec --config firebase.emulator.json --project demo-chomdoi-tests --only firestore \"npm run test:emulator\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.2",
    "typescript": "~6.0.2",
    "vite": "8.0.16",
    "vitest": "^4.1.11"
  },
  "dependencies": {
    "chart.js": "^4.5.1",
    "firebase": "^12.12.1"
  }
}
