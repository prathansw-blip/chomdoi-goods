# เตรียมข้อมูลก่อนย้ายระบบ Chomdoi Goods

เอกสารนี้เป็นขั้นเตรียมการเท่านั้น ยังไม่สร้างบัญชีหรือ `/staff/{uid}` ใน Production, ไม่ลบ `users`, ไม่เปลี่ยน Rules และไม่ deploy เว็บ

## 1. ยืนยันบัญชีพนักงาน

อ่าน `/stores/chomdoi_main.users` และ Firebase Auth แบบอ่านอย่างเดียว แล้วใช้ `scripts/migration-plan.mjs` จับคู่ `username` กับอีเมล `username@chomdoi.local` แผนนี้ไม่คัดลอกรหัสผ่าน บัญชี Auth ที่จับคู่ไม่ได้ต้องให้เจ้าของระบบตรวจด้วยตนเอง และห้ามให้สิทธิ์ `/staff/{uid}` อัตโนมัติ ต้องตรวจด้วยว่าพนักงานแต่ละคนทราบวิธีเข้าใช้ Firebase Auth; รหัสผ่านเดิมอาจใช้กับ Auth ไม่ได้

ก่อนย้ายจริง ให้เก็บรายชื่อพนักงานที่อนุมัติ บทบาท และสถานะ active เป็นรายการตรวจทานในพื้นที่ส่วนตัวนอก repository จากนั้นอ่านข้อมูล Production ใหม่ในช่วงหยุดทำรายการ เพราะบัญชีอาจเปลี่ยนหลังการตรวจครั้งนี้

## 2. เก็บข้อมูลที่อาจค้างในแต่ละเครื่อง

เว็บรุ่นเดิมเขียนลง `localStorage` คีย์ `chomdoi_goods_data` ก่อนส่ง Firestore และไม่แจ้งชัดเมื่อการส่งล้มเหลว การเปิดหรือรีโหลดเว็บรุ่นเดิมอาจอ่านข้อมูลจาก Firestore แล้วเขียนทับ cache ในเครื่อง ดังนั้น **อย่าเปิดหรือรีโหลดเว็บเพื่อเริ่มตรวจ cache** ให้หยุดใช้งานและเก็บสำเนา browser profile หรือ localStorage ของทุกเครื่อง/ทุกเบราว์เซอร์ก่อน โดยให้ผู้ดูแลช่วยตามชนิดอุปกรณ์

ข้อมูล export ของระบบเดิมมี `users` รวมรหัสผ่านเดิมและอาจมี LINE token จึงต้องเก็บและส่งต่อแบบเข้ารหัสในพื้นที่ส่วนตัว ห้ามส่งทางแชตทั่วไปหรือ commit ลง Git และห้ามลบ cache เดิมจนกว่าจะตรวจครบ

เมื่อตรวจสอบ snapshot ฝั่ง Firestore และข้อมูลจากเครื่องในรูป JSON แล้ว ใช้คำสั่งอ่านอย่างเดียวนี้บนไฟล์ที่เก็บ **นอก repository**:

```sh
rtk proxy node scripts/audit-legacy-cache.mjs /private/path/server.json /private/path/device.json
```

ผลลัพธ์แสดงเฉพาะจำนวนรายการที่มีในเครื่องแต่ไม่มีบนเซิร์ฟเวอร์, มีบนเซิร์ฟเวอร์แต่ไม่มีในเครื่อง, รายการ ID เดียวกันแต่ข้อมูลต่างกัน และสินค้าที่สต็อกต่างกัน การพบความต่างต้องตรวจรายการจริงกับเจ้าของระบบก่อนตัดสินใจแก้ยอด **อย่านำ export เก่าทั้งเอกสารไปเขียนทับ Firestore** ไฟล์ snapshot ควรตั้งสิทธิ์ให้เจ้าของเครื่องอ่านได้คนเดียวและลบอย่างปลอดภัยหลังจบงาน

## 3. สำรองและกู้คืน

สำเนาท้องถิ่นเดิมอยู่ใต้ `/Users/keng/.codex/backups/chomdoi-house/` และมีตัวตรวจความสมบูรณ์กับวิธีกู้คืนในโฟลเดอร์นั้น แต่เป็นภาพ ณ วันสำรอง ไม่ใช่ข้อมูลล่าสุด และกุญแจถอดรหัสอยู่ใน Keychain ของ Mac เครื่องเดียวกัน จึงยังไม่ใช่สำรองนอกเครื่องที่กู้คืนได้หากเครื่องสูญหาย

ก่อน cutover ต้องหยุดทำรายการ, สร้างสำเนาเข้ารหัสใหม่ของ Firestore/Auth/Rules, ตรวจว่าถอดรหัสได้, เก็บสำเนาและวิธีเข้าถึงกุญแจในปลายทางนอกเครื่องที่เจ้าของระบบเลือก และทดสอบกู้คืนกับ Emulator หรือโปรเจกต์แยกก่อนแตะ Production ห้ามกู้คืนทั้งเอกสารโดยไม่กระทบยอดรายการที่เกิดหลังเวลาสำรอง

## เกณฑ์พร้อมเข้าสู่ช่วง cutover

- เจ้าของระบบอนุมัติรายชื่อและบทบาทพนักงานที่จะมี `/staff/{uid}` และตัดสินใจเรื่องบัญชี Auth ที่ไม่จับคู่
- ตรวจทุกเครื่องที่เคยทำรายการ และกระทบยอดรายการ/สต็อกที่ต่างกับ Firestore แล้ว
- มีสำรองล่าสุดที่เข้ารหัส เก็บนอกเครื่องพร้อมกุญแจที่กู้คืนได้ และผ่านการทดสอบกู้คืน
- กำหนดช่วงหยุดทำรายการและผู้รับผิดชอบตรวจ login, ยอดขาย, สต็อก และ Rules หลัง deploy ตาม `SECURITY_ROLLOUT.md`
