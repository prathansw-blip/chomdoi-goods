# เตรียมข้อมูลก่อนย้ายระบบ Chomdoi Goods

เอกสารนี้บันทึกการเตรียมและผลย้ายระบบเมื่อ 26 กันยายน 2026: สร้าง `/staff/{uid}` 5 รายการตามรายชื่อที่อนุมัติแล้ว ลบ `users` จากเอกสารร้านและเพิ่ม `revision: 0` แล้ว deploy Firebase Hosting และ secure Rules จาก commit `22370b5` โดยไม่สร้างหรือลบบัญชี Firebase Auth และยังไม่เปลี่ยนรหัสผ่าน อีก 6 บัญชี Auth ไม่มี staff record และไม่ได้สิทธิ์เข้า webapp รุ่นใหม่ **ยังไม่เปิดรับรายการ เพราะเจ้าของรายงานว่า admin เข้าไม่ได้และกำลังตรวจสาเหตุ**

## 1. ยืนยันบัญชีพนักงาน

อ่าน `/stores/chomdoi_main.users` และ Firebase Auth แบบอ่านอย่างเดียว แล้วใช้ `scripts/migration-plan.mjs` จับคู่ `username` กับอีเมล `username@chomdoi.local` แผนนี้ไม่คัดลอกรหัสผ่าน เจ้าของระบบยืนยันให้บัญชี Auth ที่ไม่จับคู่ทั้ง 6 ไม่มีสิทธิ์ `/staff/{uid}` ก่อนเปิดใช้รุ่นใหม่ต้องให้พนักงานทดลองล็อกอินที่หน้า **webapp Chomdoi Goods** ด้วยชื่อผู้ใช้และรหัสผ่านของตน เพราะรหัสผ่านเดิมอาจใช้กับระบบตรวจตัวตนใหม่ไม่ได้ พนักงานไม่ต้องเข้า Firebase Console หรือแก้ข้อมูลใน Firebase; ผู้ดูแลระบบเป็นผู้จัดการบัญชีเบื้องหลัง

ก่อนย้ายจริง ให้เก็บรายชื่อพนักงานที่อนุมัติ บทบาท และสถานะ active เป็นรายการตรวจทานในพื้นที่ส่วนตัวนอก repository จากนั้นอ่านข้อมูล Production ใหม่ในช่วงหยุดทำรายการ เพราะบัญชีอาจเปลี่ยนหลังการตรวจครั้งนี้

ผลตรวจล่าสุด: `admin` ได้บทบาท `admin`; `ho`, `jom`, `wan`, `liyah` ได้บทบาท `user` และ active ทั้งหมดตามที่เจ้าของระบบยืนยัน หลักฐานการสร้าง staff และ UID อยู่ในไฟล์ส่วนตัว `/Users/keng/.codex/backups/chomdoi-house/staff-provision-record-2026-09-25T18-59-15-965Z.json` หลัง deploy รหัสเดิมของพนักงานทั้ง 4 ผ่าน Firebase Auth และอ่าน staff/store ผ่าน REST ได้ แต่ยังไม่ยืนยัน UI บนอุปกรณ์จริง เจ้าของลอง admin ผ่านเว็บแล้วเข้าไม่ได้ ข้อความหน้า login แยกรหัสผิดกับโหลดสิทธิ์ล้มเหลวไม่ได้ จึงยังไม่สรุปสาเหตุและยังไม่เปลี่ยนรหัส

## 2. เก็บข้อมูลที่อาจค้างในแต่ละเครื่อง

เว็บรุ่นเดิมเขียนลง `localStorage` คีย์ `chomdoi_goods_data` ก่อนส่ง Firestore และไม่แจ้งชัดเมื่อการส่งล้มเหลว การเปิดหรือรีโหลดเว็บรุ่นเดิมอาจอ่านข้อมูลจาก Firestore แล้วเขียนทับ cache ในเครื่อง ดังนั้น **อย่าเปิดหรือรีโหลดเว็บเพื่อเริ่มตรวจ cache** ให้หยุดใช้งานและเก็บสำเนา browser profile หรือ localStorage ของทุกเครื่อง/ทุกเบราว์เซอร์ก่อน โดยให้ผู้ดูแลช่วยตามชนิดอุปกรณ์

ข้อมูล export ของระบบเดิมมี `users` รวมรหัสผ่านเดิมและอาจมี LINE token จึงต้องเก็บและส่งต่อแบบเข้ารหัสในพื้นที่ส่วนตัว ห้ามส่งทางแชตทั่วไปหรือ commit ลง Git และห้ามลบ cache เดิมจนกว่าจะตรวจครบ

เมื่อตรวจสอบ snapshot ฝั่ง Firestore และข้อมูลจากเครื่องในรูป JSON แล้ว ใช้คำสั่งอ่านอย่างเดียวนี้บนไฟล์ที่เก็บ **นอก repository**:

```sh
rtk proxy node scripts/audit-legacy-cache.mjs /private/path/server.json /private/path/device.json
```

ผลลัพธ์แสดงเฉพาะจำนวนรายการที่มีในเครื่องแต่ไม่มีบนเซิร์ฟเวอร์, มีบนเซิร์ฟเวอร์แต่ไม่มีในเครื่อง, รายการ ID เดียวกันแต่ข้อมูลต่างกัน และสินค้าที่สต็อกต่างกัน การพบความต่างต้องตรวจรายการจริงกับเจ้าของระบบก่อนตัดสินใจแก้ยอด **อย่านำ export เก่าทั้งเอกสารไปเขียนทับ Firestore** ไฟล์ snapshot ควรตั้งสิทธิ์ให้เจ้าของเครื่องอ่านได้คนเดียวและลบอย่างปลอดภัยหลังจบงาน

ตรวจ Chrome บน Mac เครื่องนี้แล้วเมื่อ 26 กันยายน 2026 โดยคัดลอก Local Storage ผ่าน Finder ก่อนอ่าน และไม่เปิดเว็บร้าน: โปรไฟล์ Default มีรายการขาย 1,122 รายการ เทียบกับ Firestore 1,140 รายการ; ไม่มีรายการขายหรือการเติมสินค้าที่พบเฉพาะในเครื่อง ความต่างสต็อกทั้ง 14 สินค้าอธิบายได้จากรายการขายใหม่ 18 รายการและการเติมสินค้าใหม่ 2 รายการบน Firestore โปรไฟล์ 3 เป็น cache เก่าตั้งแต่สิงหาคม มีรายการขาย 885 รายการและไม่มีรายการที่มีเฉพาะในเครื่อง แต่มีการแก้สต็อกในช่วงเวลาที่ยาวกว่าซึ่งอธิบายด้วยรายการขาย/เติมสินค้าอย่างเดียวไม่ได้ จึงไม่ควรนำ cache นั้นกลับไปเขียน โปรไฟล์ 2 และ 4 ไม่พบข้อมูลร้าน สำเนาเฉพาะข้อมูล Chomdoi ถูกเข้ารหัสไว้ใต้ `/Users/keng/.codex/backups/chomdoi-house/chrome-cache-review-2026-09-25/`; สำเนา Local Storage ทั้งก้อนที่ใช้ตรวจชั่วคราวถูกลบแล้ว ผลนี้ครอบคลุมเฉพาะ Chrome บน Mac เครื่องนี้ หากมีเครื่องอื่นที่เคยทำรายการต้องตรวจเครื่องนั้นแยก

เจ้าของระบบระบุ Windows PC 1 เครื่องและ Android ที่ใช้ Chrome 1 เครื่อง แล้วสั่งให้เดินหน้าย้ายและ deploy โดยข้ามการตรวจ cache สองเครื่องนี้เมื่อ 26 กันยายน 2026 ข้อมูลเฉพาะเครื่องยังไม่ทราบและจะไม่ซิงก์เข้าระบบใหม่อัตโนมัติภายหลัง ให้เก็บข้อมูล Chrome เดิมไว้; หากต้องกู้รายการภายหลังต้องกระทบยอดกับฐานกลางใหม่ ห้ามนำ cache เก่าทั้งเอกสารเขียนทับ Firestore

เว็บใหม่จะพยายามเก็บรายการธุรกิจจาก cache เดิมก่อนล้างรหัสผ่านไว้ที่ `chomdoi_legacy_recovery_<timestamp>` โดยไม่มี `users`, Settings หรือ field รหัสผ่าน/token และไม่ส่งเข้าฐานกลาง หากพื้นที่ไม่พอจะเก็บรายการที่ตัดข้อมูลลับแล้วไว้ในคีย์เดิม หากข้อมูลเสียหรือเขียน storage ไม่ได้จะคงของเดิมไว้ ยังไม่ได้ยืนยันสำเนาจริงของ Windows/Android จนกว่าจะเปิดเว็บรุ่นใหม่บนเครื่องนั้น `scripts/audit-legacy-cache.mjs` รองรับ JSON envelope `chomdoi-legacy-recovery-v1` สำหรับตรวจสำเนาภายหลัง อย่าล้าง Chrome data ก่อนนำสำเนามาตรวจบน Mac

## 3. สำรองและกู้คืน

สำเนาเข้ารหัสก่อนย้ายจริงอยู่ที่ `/Users/keng/.codex/backups/chomdoi-house/2026-09-26T03-41-30-259Z-e410f9/` และหลังย้ายที่ `/Users/keng/.codex/backups/chomdoi-house/2026-09-26T03-43-14-974Z-dab1f4/` ทั้งสองชุดตรวจถอดรหัสผ่านแล้ว มีข้อมูลร้าน, `/staff` 5 รายการ, Firebase Auth 11 บัญชี, การตั้งค่า hash ของ Auth และ Firestore Rules ณ เวลาสำรอง หลังย้ายเอกสารร้านมี update time `2026-09-26T03:42:23.647120Z` และกฎเป็น secure Rules กุญแจอยู่ใน Keychain ของ Mac เครื่องเดียวกัน เจ้าของระบบเลือกเก็บสำรองบน Mac เครื่องนี้เท่านั้น จึงยอมรับข้อจำกัดว่าหาก Mac หรือ Keychain สูญหายพร้อมกัน อาจกู้ข้อมูลจากสำเนานี้ไม่ได้

ซ้อมกู้คืนสำเนาชุดวันที่ 25 กันยายน `/Users/keng/.codex/backups/chomdoi-house/2026-09-25T19-00-49-072Z-e3a0fa/` ใน Firestore Emulator บน Mac ผ่านแล้ว โดยตัดรหัสผ่านเดิมและแทน LINE token ด้วยค่าจำลองก่อนนำเข้า ตรวจ 14 สินค้า, 1,140 รายการขาย และ 5 staff ครบ พร้อมซ้อมลบ `users`/เพิ่ม `revision` ด้วย update mask และ precondition ไม่ได้ซ้อมนำเข้า Auth hash และการซ้อมไม่ได้เขียน Firestore Production

ย้ายจริงหลังเจ้าของยืนยันหยุดทำรายการและอนุมัติข้าม Windows/Android โดยเปิด maintenance Rules และตรวจว่าปิดการอ่านจริงก่อนแก้เอกสาร เขียนด้วย update mask เฉพาะ `users`/`revision` และ precondition ตาม `updateTime` ทุกฟิลด์อื่นเท่ากับก่อนเขียน รวม 14 สินค้าและ 1,143 รายการขาย หลังจากนั้น deploy เว็บและ secure Rules และตรวจไฟล์เว็บ/Rules source ตรงกับ build/repository ห้ามกู้คืนทั้งเอกสารโดยไม่กระทบยอดรายการที่เกิดหลังเวลาสำรอง

## งานที่เหลือก่อนเปิดรับรายการ

- ตรวจและแก้สาเหตุที่ admin เข้าไม่ได้ แล้วให้เจ้าของยืนยันการเข้า webapp จริง รวมถึงทดสอบ UI ของพนักงาน ส่วนบัญชี Auth อีก 6 รายการยังไม่ให้สิทธิ์ staff
- Mac เครื่องนี้ตรวจแล้ว; เจ้าของระบบสั่งข้ามการตรวจ Windows/Android ตามข้อยกเว้นใน `SECURITY_ROLLOUT.md` โดยข้อมูลเฉพาะสองเครื่องยังไม่ยืนยัน
- สำรองก่อน/หลังย้ายจริงตรวจถอดรหัสได้แล้ว การซ้อมกู้คืนข้อมูลจำลองผ่านแล้ว แต่ยังไม่ได้ซ้อมนำเข้า Auth hash
- เมื่อ admin ผ่าน ให้ทุกเครื่องปิดแท็บเก่าแล้วเปิด URL เดิมเพื่อโหลดรุ่นใหม่ โดยไม่ล้าง Chrome data; ตรวจยอดขาย/สต็อกก่อนเปิดรับรายการตาม `SECURITY_ROLLOUT.md`
